services:
  - type: web
    name: dbmaster-studio
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: node src/dist/server.cjs
    publishDir: src/dist
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        sync: false
      - key: ADMIN_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false